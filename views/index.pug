extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href="/new") New Message

  dl
    each message in messages
      dt #{message.user} |  #{message.added}
      dd #{message.text}
